<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View" />
        <variable
            name="dialog"
            type="com.kyad.traystorage.app.ocr.OcrEditDialog" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alpha="0.6"
            android:background="@color/black"
            android:onClick="@{()->dialog.onClickOutside()}" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginHorizontal="24dp"
            android:background="@drawable/shape_dialog_round"
            android:orientation="vertical"
            android:padding="20dp">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <com.kyad.traystorage.app.common.font.FontTextView
                    android:id="@+id/title"
                    style="@style/font_spoqahansansneo_bold"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerVertical="true"
                    android:includeFontPadding="false"
                    android:text="OCR 텍스트 편집"
                    android:textColor="@color/black"
                    android:textSize="18sp" />

                <ImageView
                    android:id="@+id/btn_close"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:onClick="@{()->dialog.onClickCancel()}"
                    android:src="@drawable/ic_close"
                    android:contentDescription="닫기" />
            </RelativeLayout>

            <com.kyad.traystorage.app.common.font.FontTextView
                android:id="@+id/status_text"
                style="@style/font_spoqahansansneo_regular"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:includeFontPadding="false"
                android:text="이미지에서 추출한 텍스트를 편집할 수 있습니다."
                android:textColor="@color/C777777"
                android:textSize="13sp" />

            <ProgressBar
                android:id="@+id/progress_bar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:layout_marginTop="12dp"
                android:visibility="gone"
                android:indeterminate="true" />

            <ScrollView
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:layout_marginTop="12dp"
                android:fillViewport="true"
                android:minHeight="200dp"
                android:maxHeight="400dp">

                <EditText
                    android:id="@+id/edit_ocr_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_edittext"
                    android:gravity="top|start"
                    android:hint="OCR 결과가 여기에 표시됩니다..."
                    android:inputType="textMultiLine"
                    android:minHeight="200dp"
                    android:padding="12dp"
                    android:textColor="@color/black"
                    android:textColorHint="@color/CBFBFBF"
                    android:textSize="14sp" />
            </ScrollView>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:gravity="end"
                android:orientation="horizontal">

                <com.kyad.traystorage.app.common.font.FontTextView
                    android:id="@+id/btn_cancel"
                    style="@style/font_spoqahansansneo_bold"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginEnd="12dp"
                    android:background="@drawable/bg_btn_cancel"
                    android:gravity="center"
                    android:includeFontPadding="false"
                    android:onClick="@{()->dialog.onClickCancel()}"
                    android:paddingHorizontal="20dp"
                    android:text="취소"
                    android:textColor="@color/focus_color"
                    android:textSize="14sp" />

                <com.kyad.traystorage.app.common.font.FontTextView
                    android:id="@+id/btn_confirm"
                    style="@style/font_spoqahansansneo_bold"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:background="@drawable/bg_btn_confirm_bottom"
                    android:gravity="center"
                    android:includeFontPadding="false"
                    android:onClick="@{()->dialog.onClickConfirm()}"
                    android:paddingHorizontal="20dp"
                    android:text="적용"
                    android:textColor="@color/white"
                    android:textSize="14sp" />
            </LinearLayout>

        </LinearLayout>
    </RelativeLayout>
</layout>
